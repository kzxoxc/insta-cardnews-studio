# 카드뉴스 스타일 02 — 포토 스카이 (Photo Sky)

> 인스타그램 카드뉴스 디자인 기준 문서 (Claude Code 빌드용)
> **분위기 키워드: 청량함 · 사실감 · 정보 신뢰**

스타일 01(스카이 미니멀)의 팔레트·폰트를 그대로 쓰되, **배경에 사진을 깐다.**
1장은 공고 포스터, 2장부터는 주제에 맞는 AI 생성 이미지.

**스타일 01은 그대로 살아 있다.** 글자만으로 가는 게시물은 01, 사진을 쓰는 공고형은 02.

---

## 1. 색상 팔레트

스타일 01과 **동일**하다. 아래는 사진 위에서 달라지는 부분만.

| 역할 | 값 | 비고 |
|---|---|---|
| 제목 텍스트 | `#0F2C44` | 01과 동일 |
| 본문 텍스트 | `#4E6B82` | 01과 동일 |
| 강조색 | `#1F7AC0` | 01과 동일. **제목 한 곳 + 본문 한 곳, 카드당 최대 두 곳** |
| 스크림(사진 위 막) | `linear-gradient(160deg, #F0F7FC, #DCEAF6 52%, #C2DCEF)` 를 알파로 | 아래 §3 |
| pill 배경 | `rgba(255,255,255,.82)` | 01(.72)보다 조금 진하게 — 사진 위에서 떠 보이게 |

> 텍스트 색을 흰색으로 바꾸지 않는다. 스카이 톤을 유지해야 01과 한 계정으로 보인다.

## 2. 폰트

스타일 01과 **완전히 동일**. Pretendard(300/400/500/600/700/800) + Space Grotesk.
로컬 `build/fonts/` 파일을 `@font-face`로 물린다. 폴백 금지.

## 3. 사진 배경 처리 — 이 스타일의 핵심

사진은 **분위기만** 담당한다. 정보는 전부 텍스트가 전달한다.

```css
/* 배경 레이어가 글자를 덮지 않도록 z-index 를 반드시 명시한다.
   absolute 요소는 정적 콘텐츠보다 나중에 그려져, 빼먹으면 글자가 통째로 사라진다. */
.card > *{position:relative;z-index:2;}

/* 1) 사진: 화면을 덮되 흐리게. zoom 으로 글자 영역을 프레임 밖으로 밀어낸다 */
.bg{position:absolute;inset:0;z-index:0;background-size:cover;
    background-position:var(--fx) var(--fy);
    filter:blur(var(--blur));transform:scale(var(--zoom));}

/* 2) 스크림: 01의 그라데이션을 알파로 덮는다 */
.scrim{position:absolute;inset:0;z-index:1;
  background:linear-gradient(160deg,
    rgba(240,247,252,var(--a)) 0%,
    rgba(220,234,246,calc(var(--a) - .04)) 52%,
    rgba(194,220,239,var(--a)) 100%);}
```

**기준값** (실제 렌더로 맞춘 값. 카드별로 `scrim` / `blur` / `zoom` / `focusX` / `focusY` 필드로 덮어쓸 수 있다)

| 카드 | `--a` 스크림 | `--blur` | `--zoom` | 이유 |
|---|---|---|---|---|
| 1장 (포스터) | `.62` | `2px` | `2.0` | 어떤 공고인지 알아볼 수 있어야 한다 |
| 내용 카드 | `.84` | `8px` | `1.08` | 본문이 길어 대비가 더 필요 |
| 마지막 CTA | `.82` | `7px` | `1.08` | 중간 |

- 스크림은 **0까지 내릴 수 있다** (하한 없음). 0에 가까우면 하늘색 막 없이 사진이 그대로
  드러난다. 대신 `.photo .title/.sub/.eyebrow` 등에 고정 `text-shadow`를 깔아둬서 어떤
  사진이든 최소한의 대비를 보장한다 — 스크림이 충분히 높으면 이 그림자는 육안에 거의 안 보인다.
  본문(`#4E6B82` Light 38px)이 제목보다 먼저 깨지는 건 여전하니, 사진이 밝고 복잡하면
  스크림을 더 올려서 본문 대비를 챙긴다.
- **선명하게 갈수록 `zoom`을 키워야 한다.** 블러가 약하면 포스터 글자가 그대로 읽힌다.
  blur 7 / zoom 1.5 로 뽑았을 때 상단 "앰버서더란?", 하단 "서류지원 7/20…" 이 카드 텍스트·
  푸터와 겹쳤다. **blur 2 로 내릴 때는 zoom 2.0** 이 필요하다.
- `focusX`/`focusY`(=`background-position`의 가로·세로 값, 0~100)로 그림이 있는 부분을
  프레임 중앙에 맞춘다. 세로 포스터는 대개 `focusX:50, focusY:50`. 구버전 `focus`(세로값 하나)
  필드도 하위호환으로 읽는다. 웹앱 편집 화면에서 사진을 직접 드래그(위치)·모서리를 대각선으로
  드래그(확대)해서 눈으로 맞출 수 있다 — 수치를 몰라도 된다.
- **사진 위에서는 옅은 글자의 대비를 올린다** (`.photo` 규칙). 영문 라벨 `#456780`,
  페이지 번호 `#6B8AA2`, 헤어라인 `rgba(15,44,68,.24)`. 안 올리면 사진에 묻혀 안 보인다.
- 사진이 없으면 스크림도 그리지 않는다 = 스타일 01과 동일한 화면. 이게 폴백이다.

## 4. 레이아웃

**스타일 01과 동일**하다 (01 §3). 사진이 들어와도 글자 위치는 바뀌지 않는다.

- **좌측 정렬 + 하단 앵커.** 위쪽 빈 공간을 배경 사진이 채운다. 이 레이아웃은 사진이 있을 때
  가장 잘 맞는다.
- 안전 여백 상하 90 / 좌우 80, 본문 끝에 128px 여백
- 하단 푸터는 `position:absolute; bottom:90px` 고정
- **마지막 팔로우 카드만 중앙 정렬.** 인스타 프로필 목업(`.pcard`)을 얹어 팔로우를 청한다.
  프로필 사진은 `build/assets/profile.png` 공용 자산을 쓴다 (미리보기 `/asset/`, 렌더 `file:///`).

### 내용 카드의 항목 라벨
공고형에서는 그 장이 무슨 항목인지 상단에 pill 로 밝힌다 (`label` 필드).
`모집대상` `활동내용` `혜택` `사전 과제` `활동일정` `지원방법` — `brief`의 키와 맞춘다.
읽는 사람이 넘기다가 원하는 항목을 찾을 수 있어야 한다.

## 5. 카드 비율

- **4:5 세로형 = 1080 × 1350 px**, 렌더 배율 2 → **2160 × 2700**
- 배경 사진 원본은 **가로세로 어느 쪽이든 1080px 이상**. 그 아래면 블러를 걸어도 뭉갠 티가 난다.

## 6. 사진을 구하는 방법

1. **1장 = 공고 포스터.** 앱이 링크의 `og:image` / 본문 이미지를 자동으로 받아
   `work/<슬러그>/images/poster.*` 에 저장한다.
2. **2장부터 = AI 생성 이미지.** 앱이 카드마다 영문 프롬프트를 보여준다.
   사용자가 원하는 도구로 생성한 뒤 그 카드에 올리면 된다.

### AI 이미지 프롬프트 작성 규칙
- **영문으로 쓴다.** 대부분의 생성 도구가 영문에서 결과가 안정적이다.
- **글자를 넣지 말라고 명시한다** (`no text, no letters, no logos`). 카드에 이미 글자가 있다.
- **하늘색·푸른 톤을 지정한다** (`soft sky blue palette, airy, bright`). 팔레트와 충돌하면 안 된다.
- **인물 얼굴 클로즈업을 피한다.** 블러 뒤에서 이상하게 보이고, 초상권 인상도 준다.
- 카드 내용의 **주제**를 담되 문구를 그대로 옮기지 않는다.
- 세로 4:5 또는 정사각으로 뽑는다.

예시 (활동 내용 = SNS 영상 제작):
```
A young person filming a short vertical video with a smartphone on a tripod,
minimal bright workspace, soft sky blue and white palette, airy natural light,
shallow depth of field, no text, no letters, no logos, editorial photography, 4:5
```

## 7. 금지 사항

- 사진 위에 **검은색 반투명 막**을 쓰지 않는다. 스카이 톤이 무너진다.
- 사진에 들어 있는 글자를 정보로 쓰지 않는다. **읽혀야 하는 내용은 반드시 텍스트로 다시 쓴다.**
- 강조색은 **여전히 제목 한 곳 + 본문 한 곳, 카드당 최대 두 곳**. 사진이 들어왔다고 더 늘리지 않는다.
- 도트·반짝이 등 장식 패턴 금지 (01과 동일).
- 사진을 선명하게 두고 그 위에 글자를 얹지 않는다. 반드시 블러 + 스크림을 거친다.

## 8. 분위기 키워드

**청량함 · 사실감 · 정보 신뢰**
