# 카드뉴스 스타일 01 — 스카이 미니멀 (Sky Minimal)

> 인스타그램 카드뉴스 디자인 기준 문서 (Claude Code 빌드용)
> **분위기 키워드: 청량함 · 세련됨 · 미니멀**

맑은 하늘색 배경 위에 군더더기 없는 산세리프. 파스텔 특유의 귀여움을 걷어내고 **여백과 타이포만으로** 인상을 만든다.

---

## 1. 색상 팔레트

| 역할 | HEX | 비고 |
|---|---|---|
| 배경 (base) | `#DCEAF6` | 연하늘 단색 |
| 배경 (gradient 옵션) | `#F0F7FC → #C2DCEF` (160deg) | 위=밝게 아래=진하게, 아주 은은하게 |
| 제목 텍스트 | `#0F2C44` | 딥 네이비 |
| 본문 텍스트 | `#4E6B82` | 블루 그레이 |
| 강조색 (accent) | `#1F7AC0` | 클리어 블루 (포인트 단어) |
| 강조 딥 | `#0F4C7A` | 태그 글자 |
| 보조선/보더 | `rgba(15,44,68,.14)` | 1px 헤어라인 |
| 카드/pill 배경 | `rgba(255,255,255,.72)` | 반투명 화이트 |

배경 CSS:
```css
background: linear-gradient(160deg, #F0F7FC 0%, #DCEAF6 52%, #C2DCEF 100%);
```

> **도트·반짝이 등 장식 패턴은 쓰지 않는다.** 질감이 필요하면 opacity .04 이하의 미세 노이즈까지만.

## 2. 폰트

| 용도 | 폰트 | 굵기 | 출처 |
|---|---|---|---|
| 제목 | **Pretendard** | ExtraBold (800) | github `orioncactus/pretendard` (SIL OFL) |
| 본문 | **Pretendard** | Light / Regular | 위와 동일 |
| 태그·라벨 | **Pretendard** | SemiBold (600) | 위와 동일 |
| 영문 포인트 | **Space Grotesk** | Medium/Bold | Google Fonts (OFL) `ofl/spacegrotesk/...` |

> 제목은 자간을 좁게(`letter-spacing:-.03em`), 영문 라벨은 넓게(`.3em` 이상) 잡아 대비를 만든다.

## 3. 레이아웃

- **정렬: 좌측 정렬(left).** 글덩어리는 카드 **아래쪽에 앵커**하고 위를 비운다.
  - 전부 중앙에 몰아넣으면 캐러셀을 넘길 때 화면이 다 똑같아 보인다. 실제로 그렇게 만들었다가
    "너무 일편적"이라는 지적을 받아 바꿨다.
  - **예외: 마지막 팔로우 카드만 중앙 정렬**(`.card.center`). 마무리에서 리듬을 끊어준다.
- 세로 배치: 위→아래로 `영문 라벨 / 번호 → 태그 또는 항목 라벨 → (빈 공간) → 메인 제목 → 서브
  → (하단 고정) 헤어라인 + 워터마크`.
- 위쪽 빈 공간은 스타일 02에서 배경 사진이 채운다. **사진 없이 이 레이아웃을 쓰면 위가 허전해
  보인다** — 글자만 갈 거면 사진을 넣거나 중앙 정렬로 되돌린다.
- **요소 수 최소화.** 블록 사이 여백을 크게 잡아 밀도를 낮춘다.
- 장식은 헤어라인(1px) 하나까지만. 이모티콘·반짝이 금지.
- 푸터는 `position:absolute; bottom:90px`. 본문 끝에 **128px 여백**을 둬서 겹치지 않게 한다.

## 4. 카드 구성요소

- **태그**: 반투명 흰 배경(`rgba(255,255,255,.72)`) + 1px 보더, radius 999px(pill), 글자 `#0F4C7A` SemiBold, 2~3개 중앙 정렬로 나란히.
- **제목**: `대외활동 계정`(딥 네이비) + `시작 !!`(강조 `#1F7AC0`, 초대형). 두 줄로 끊어 쓴다.
- **영문 라벨**: `START · 2026` 같은 짧은 문구, Space Grotesk 대문자, 자간 넓게, 색 `#7C97AD`.
- **내용(서브)**: 본문 컬러, Light, 줄간 1.6.
- **하단 영역**: 1px 헤어라인 위/아래에 `@아이디`(Space Grotesk) + 페이지 마커. 목업 프로필을 쓸 경우 radius 20px·그림자 최소로 절제.

## 5. 카드 비율

- **4:5 세로형 = 1080 × 1350 px**
- 안전 여백: 상하 90px / 좌우 80px (미니멀은 여백을 넉넉히)
- 렌더 배율 `device_scale_factor = 2` (2160×2700 출력)

## 6. 분위기 키워드

**청량함 · 세련됨 · 미니멀**

---

### 구현 참고
- 폰트는 CDN 대신 로컬 otf/ttf를 받아 `@font-face`로 물린다.
- 스크린샷 전 `document.fonts.ready` 대기 (한글 폴백 방지).
- `.card` 요소 단위로 `element.screenshot()`.
- 강조색은 **제목에 한 곳, 본문에 한 곳 — 카드당 최대 두 곳**. 한 필드 안에 `*..*`를 여러 번 써도
  첫 번째만 살고 나머지는 무시된다. 여러 군데 칠하면 미니멀함이 깨진다.
